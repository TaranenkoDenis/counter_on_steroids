name: counter_on_steroids
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  freezed_annotation: ^1.0.0
  flutter_bloc: ^8.0.0
  path_provider: ^2.0.7
  bloc_test: ^9.0.1
  shared_preferences: ^2.0.9


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  mockito: ^5.0.16
  freezed: ^1.0.0
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
